<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CustomTheme" parent="Theme.AppCompat.CompactMenu">
        <item name="android:listViewStyle">@style/CustomListView</item>
        <item name="android:textViewStyle">@style/CustomTextView</item>
    </style>

    <style name="CustomListView">
        <item name="android:background">@drawable/gradient</item>
        <item name="android:fastScrollEnabled">true</item>
    </style>

    <style name="CustomTextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:typeface">monospace</item>
        <item name="android:textColor">@color/colorGreen</item>
    </style>

    <style name="CuctomThemeGenresMovie" parent="Animation.AppCompat.DropDownUp">
        <item name="android:gridViewStyle">@style/GridViewStyle</item>
    </style>

    <style name="GridViewStyle">
        <item name="android:background">@drawable/gradient_grid</item>
    </style>

    <style name="TextGridView">
        <item name="android:textColor">@color/colorBrown</item>
        <item name="android:textSize">@dimen/text_size</item>
    </style>

</resources>
